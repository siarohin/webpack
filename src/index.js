/**
 * Main entry point for the application. Imports all modules, that
 * will be included in main.bundle.js
 *
 * How to import:
 * import json from "./assets/demo.json";
 *
 * Configure webpack.config.js in case using alias, example:
 * import { Demo } from "./models/index";
 * can be imported as
 * import { Demo } from "@models/index";
 */
import "./styles/styles.scss";
